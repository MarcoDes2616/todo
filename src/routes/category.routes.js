const { Router } = require("express");
// const { createUser } = require("../controllers/users.controllers");


const router = Router();

// crear categoria
router.post("/api.vi/cate")


module.exports = router;